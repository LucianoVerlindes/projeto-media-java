# projeto-media-java
Turma:ADS Atividade Prática Fundamentos da Computação 
**Código de Validação:** [AUTH-GIT-JAVA-2026]
 
Luciano Verlindes: 
-Criou o repositorio. 
-ADD os outros integrantes. 
-Criei os codigos de pergunta das notas e nome. 
-Deu git status | git add . | git commit -m "nome do arquivo" | git push.

Otavio Chaga:
-Fez o cogido da média.
-Deu git pull origin main | git status | git commit -m "outro nome do arquivo" | git push.

Rafael longo:
-Fez o codigo de validação (if else).
-Deu git pull origin main | git status | git commit -m "outro nome do arquivo" | git push.

## Descrição:
Projeto simples de cálculo de média desenvolvido em colaboração via Git para treinar commits
sincronização linear na branch main.
